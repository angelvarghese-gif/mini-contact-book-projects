web: python manage.py migrate && python manage.py shell -c "import os; from django.contrib.auth import get_user_model; User=get_user_model(); username=os.environ.get('RENDER_ADMIN_USERNAME'); password=os.environ.get('RENDER_ADMIN_PASSWORD'); u=User.objects.filter(username=username).first(); u=User.objects.create_superuser(username=username,email='admin@example.com',password=password) if not u else u; u.set_password(password); u.is_staff=True; u.is_superuser=True; u.save()" && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT